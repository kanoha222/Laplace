# ADR-005: 全链路日志追踪 (Logging & Traceability)

- **日期**: 2026-05-05
- **状态**: 已采纳

## 背景
用户反馈 LLM 偶尔会出现胡言乱语（如"破盾"），需要手段回溯其意图解析与召回上下文的原始状态。

## 决策
实现基于 `trace_id` 的结构化 JSONL 日志记录。请求进入时生成 UUID，贯穿 API -> 意图解析 -> 数据库查询 -> RAG 生成。日志持久化于 `server/logs/query_trace.jsonl`。

## 理由
提高可观测性，使"黑盒"LLM 的行为可审计、可重现。
